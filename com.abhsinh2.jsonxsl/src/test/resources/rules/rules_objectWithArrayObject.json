{
    "rules" : {
        "main": "<root>{root}</root>",
        "root": {
        	"main": "<name>{$name}</name><addresses>{addresses}</addresses>",
        	"addresses": {
            	"main": "<address><city>{$city}</city><zip>{$zip}</zip></address>"
        	}
        }        
    }
}