{
    "rules" : {
    	"parameters": ["xName"],
        "main": "<root>{root}</root>",
        "root": {
        	"condition": "eq({$$xName},'test1')",
            "main": "<name>{$name}</name><address>{address}</address>",
            "address": {
            	"main": "<city>{$city}</city><zip>{$zip}</zip>"
            }
        }
    }
}