{
    "rules" : {
        "main": "<root>{root}</root>",
        "root": {
            "main": "<name>{$name}</name><address>{address}</address>",
            "address": {
            	"main": "<city>{$city}</city><zip>{$zip}</zip>"
            }
        }
    }
}